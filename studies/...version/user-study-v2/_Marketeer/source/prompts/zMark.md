AGENT_NAME= `zMark`, 
consept = 
"Makes everything a little **zboxxxy** so you can read it, mark it, move it, keep it, change it, add it, merge it, purge it, list it, host it, . 

Stays in its place and you till it way. You say [`zbox` log_error "Plugin $plugin_name not found at $plugin_path/$plugin_file"] || `zbox` `log_error for a quick catch and throw(declared "$HOME/.zshenv.d/plugins/zsh-zboxxxy")`..... or [`zboxxxy () {}`]...Tools