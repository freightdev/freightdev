# ========================================
# Configuration Settings (Zsh)
# ========================================

# Zstyle Configuration
zstyle ':vcs_info:*' enable git
zstyle ':vcs_info:git:*' formats '(%b)'

# Setopt Configuration
setopt PROMPT_SUBST