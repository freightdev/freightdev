#! /usr/bin/env zsh
#  ╔══════════════════════════╗
#?   ZSH_KEY::SCRIPT - v1.0.0
#  ╚══════════════════════════╝

#!     HOME PATHS/SETTINGS
# ================================
: "${SCRIPT_DIR:=$(dirname "$(realpath "${(%):-%N}")")}"
#: "${SCRIPT_DIR:=$(dirname "$(realpath "${(%):-%N}")")}"
#?  Add more if needed